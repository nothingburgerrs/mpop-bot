[project]
name = "mpopbot"
version = "0.1.0"
description = "Multiplayer K-pop management sandbox Discord bot"
requires-python = ">=3.11"
dependencies = [
    "discord.py>=2.5.2",
    "python-dotenv>=1.0.1",
    "Pillow>=10.0.0",
    "aiohttp>=3.9.0",
]
